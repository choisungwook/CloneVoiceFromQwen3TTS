# Python 코드 규칙

## 들여쓰기

- indent는 스페이스 2칸을 사용한다.

## 가독성

- 로직을 함수 단위로 분리하여 가독성을 높인다.
- 함수 하나는 하나의 역할만 수행한다.
- 쓸모없는 주석을 달지 않는다. 코드 자체가 의도를 설명하도록 작성한다.
- 주석은 "왜(why)"를 설명할 때만 사용한다. "무엇(what)"은 코드로 표현한다.

## 설계

- 상태 관리나 관련 동작을 묶어야 할 때는 클래스를 사용한다.
- 단순 유틸리티는 함수로 충분하다. 불필요한 클래스를 만들지 않는다.
